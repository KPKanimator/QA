# Вставка из буфера обмена в Excel без форматирования

## Что делает этот скрипт?
Он перехватывает сочетание клавиш `Ctrl`+`q`, и при нажатии этого сочетания в Excel скрипт вставляет содержимое буфера обмена **без форматирования**.

## Зачем это вообще нужно?
В Excel нет возможности изменить стандартное поведение `Ctrl`+`v`, поэтому он всегда вставляет данные с форматированием, если оно есть в источнике (например, при копировании из Word или с веб-сайтов).

Способы удаления форматирования после вставки громоздки и отнимают драгоценные микросекунды (вам нужно либо использовать мышь, чтобы изменить параметры вставки на "только текст", либо использовать несколько дополнительных сочетаний клавиш).

Это начинает раздражать, когда нужно быстро или часто вставлять "чистый" текст. Этот скрипт решает данную проблему.

## Как использовать этот скрипт?
1. Вам нужно установить **[AutoHotkey v1](https://www.autohotkey.com/)** (только для Windows). Это программа для перехвата нажатий клавиш и назначения им различных функций.
2. Скачайте файл **`Excel-paste-without-formatting.ahk`** (см. выше в репозитории).
3. Сохраните его и дважды кликните по нему для запуска. 
4. При необходимости можно добавить этот файл в автозагрузку Windows.

> *Когда скрипт работает в фоновом режиме (вы можете убедиться в этом, проверив системный трей — там должен быть зеленый значок с буквой **H**), нажатие `Ctrl`+`q` в Excel вставит содержимое буфера обмена без форматирования.*

---

## Google привел вас сюда таинственными путями?
Если вы попали сюда случайно и не хотите использовать никакие скрипты, а просто хотите вставить текст без форматирования, вот штатные способы:

### Вариант 1: Использование мыши или клавиши Ctrl
После вставки содержимого из буфера обмена рядом с ячейкой появится маленький значок планшета (смарт-тег вставки). 
1. Нажмите на него или нажмите клавишу `Ctrl`. Откроется меню.
2. Нажмите клавишу `v` (или `З` в русской раскладке), когда это всплывающее меню открыто. Это переключит режим вставки на "без форматирования" (только значения).

### Вариант 2: Ctrl + Alt + v
1. Скопируйте содержимое в буфер обмена.
2. В Excel нажмите `Ctrl` + `Alt` + `v`. Откроется окно "Специальная вставка".
3. Находясь в этом окне, вместо мыши можно нажать `Alt` + `v` *(в некоторых версиях достаточно просто `v`; для русской версии Excel выберите "Значения")*.
4. Нажмите `Enter`.

---

## Хотите использовать другое сочетание клавиш?
Если `Ctrl`+`q` вам не подходит, вы можете изменить его на любое другое в строке 7 кода скрипта.

Распространенные специальные клавиши, которые там используются:
* `+` — Shift
* `!` — Alt
* `^` — Ctrl

Таким образом, чтобы использовать `Ctrl` + `Alt` + `w`, вам нужно изменить эту строку с:
```autohotkey
`^q::`
to:
`^!w::`
```
---

# 📋 Excel VBA: Smart Paste Text Only

**PasteTextOnly** — это VBA-макрос для Excel, предназначенный для вставки содержимого буфера обмена **без форматирования**. 

Макрос автоматически определяет источник данных (внутри Excel или из внешней программы) и применяет соответствующий метод вставки, чтобы избежать переноса стилей, шрифтов, цветов и границ ячеек.

---

## 🚀 Возможности
* **Умное определение источника:** Различает копирование внутри Excel и копирование извне (браузер, Word, Outlook).
* **Excel ➞ Excel:** Вставляет только значения (`xlPasteValues`), превращая формулы в результаты и игнорируя форматирование. Автоматически снимает выделение ("бегущих муравьев").
* **Внешний источник ➞ Excel:** Пытается вставить текст как `Unicode Text` (чтобы сохранить кириллицу и спецсимволы). Если это невозможно, использует обычный текстовый формат.
* **Безопасность:** Включает обработку ошибок (`On Error Resume Next`), чтобы макрос не "падал", если буфер обмена пуст или имеет несовместимый формат.

## 💻 Код (VBA)

```vba
Sub PasteTextOnly()
    ' Макрос для вставки ТОЛЬКО ТЕКСТА (без форматирования)
    
    ' Если возникнет ошибка (например, буфер пуст), макрос просто остановится, а не выдаст окно с ошибкой
    On Error Resume Next

    ' ПРОВЕРКА: Откуда скопированы данные?
    ' Application.CutCopyMode возвращает True (число), если ячейки скопированы внутри Excel
    If Application.CutCopyMode Then
        ' СЦЕНАРИЙ 1: Копируем из Excel в Excel
        ' Вставляем только значения (убирает цвета, шрифты и превращает формулы в результат)
        Selection.PasteSpecial Paste:=xlPasteValues
        
        ' Очищаем буфер обмена (убираем "бегущих муравьев" вокруг скопированной области)
        Application.CutCopyMode = False
    Else
        ' СЦЕНАРИЙ 2: Копируем из внешнего источника (Браузер, Word, Outlook)
        ' Попытка 1: Вставить как Юникод-текст (сохраняет спецсимволы и кириллицу)
        ActiveSheet.PasteSpecial Format:="Unicode Text", Link:=False, DisplayAsIcon:=False
        
        ' Если попытка 1 не удалась (ошибка), пробуем обычный текст
        If Err.Number <> 0 Then
            ActiveSheet.PasteSpecial Format:="Text", Link:=False, DisplayAsIcon:=False
        End If
    End If
    
    ' Сбрасываем обработчик ошибок
    On Error GoTo 0
End Sub
```

## 🛠 Установка и использование

### Вариант 1: Сделать макрос доступным во всех файлах Excel (Рекомендуется)
Чтобы скрипт работал в любой открытой книге Excel, его нужно добавить в **Личную книгу макросов (Personal.xlsb)**.

1.  **Включите вкладку "Разработчик" (если нет):**
    * *Файл* ➝ *Параметры* ➝ *Настроить ленту*.
    * Поставьте галочку напротив пункта **Разработчик** справа.
2.  **Запишите пустой макрос (для создания Personal.xlsb):**
    * На вкладке *Разработчик* нажмите **Запись макроса**.
    * В поле "Сохранить в:" выберите **Личная книга макросов**.
    * Нажмите ОК, затем сразу **Остановить запись**.
3.  **Добавьте код:**
    * Нажмите `Alt` + `F11`, чтобы открыть редактор VBA.
    * Слева в панели *Project Explorer* найдите `VBAProject (PERSONAL.XLSB)`.
    * Раскройте папку `Modules` и дважды кликните на `Module1` (или создайте новый: *Insert* ➝ *Module*).
    * Вставьте код макроса `PasteTextOnly` в открывшееся окно.
    * Нажмите `Ctrl` + `S` для сохранения.

### Вариант 2: Использовать только в одном файле
1.  Откройте нужный файл Excel.
2.  Нажмите `Alt` + `F11`.
3.  В меню выберите **Insert** ➝ **Module**.
4.  Вставьте код.
5.  **Важно:** Сохраните файл в формате **Книга Excel с поддержкой макросов (.xlsm)**.

---

## ⌨️ Настройка горячих клавиш (Shortcut)
Самый удобный способ запускать этот макрос — назначить ему привычное сочетание клавиш (например, как в браузере `Ctrl`+`Shift`+`V` или `Ctrl`+`Shift`+`I`).

1.  В Excel нажмите `Alt` + `F8` (откроется список макросов).
2.  Выберите `PasteTextOnly`.
3.  Нажмите кнопку **Параметры...** (Options).
4.  В поле "Сочетание клавиш" введите желаемую букву.
    * *Совет:* Чтобы задать сочетание с `Shift` (например, `Ctrl`+`Shift`+`V`), зажмите `Shift` и нажмите клавишу `V`.
5.  Нажмите **ОК**.

## 🖱 Добавление кнопки на панель (Опционально)
Вы можете добавить кнопку в "Панель быстрого доступа" (вверху слева):

1.  Нажмите на стрелочку вниз в самом верху окна Excel (Панель быстрого доступа) ➝ **Другие команды...**
2.  В выпадающем списке "Выбрать команды из:" выберите **Макросы**.
3.  Найдите `PasteTextOnly` и нажмите кнопку **Добавить >>**.
4.  *(По желанию)* Нажмите **Изменить...**, чтобы выбрать иконку для кнопки.
5.  Нажмите **ОК**.

---

## ▶️ Как это работает
1.  Скопируйте данные откуда угодно (`Ctrl` + `C`).
2.  Выберите ячейку в Excel, куда нужно вставить данные.
3.  Нажмите заданное сочетание клавиш (или кнопку макроса).
4.  **Результат:** Данные вставятся как чистый текст, формат ячейки не изменится.
